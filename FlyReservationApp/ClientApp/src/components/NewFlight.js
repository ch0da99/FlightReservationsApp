import React from "react";
import { connect } from "react-redux";

const NewFlight = () => {
  return <div>New Flight</div>;
};

const mapStateToProps = (state) => {};

const mapDispatchToProps = {};

export default NewFlight;
